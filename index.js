const multiplyBy = (arr, number) => {
  const multiplyNew = arr.map((item) => item * number);
  return multiplyNew;
  // Zaimplementuj zadanie zgodnie z wytycznymi
};

module.exports = multiplyBy;
/*const numbersDoubled = numbers.map((item) => {
  return item * 2;
});*/
